{
  "name": "the-missing-20",
  "version": "0.1.0",
  "private": true,
  "description": "Agentic supply-chain exception resolution with evidence-grounded diagnosis and controlled recovery.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests-js/*.test.mjs",
    "check": "npm test"
  }
}
