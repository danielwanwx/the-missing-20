# D4 actual live screen — independent semantic review

Verdict: **FAILED; stop this frozen sequence.** Four of six questions were reached. Q5 and Q6, the source transition, and the live process-restart branch are **NOT_REACHED**. Runtime COMPLETE for Q4 did not establish a correct answer. This does not revoke the separately reviewed S2 structural persistence evidence, and does not accept D4/D5 or finalization.

Reviewed execution revision: `4706a9245f33b715442444b8520290b4f40f2c27`. The frozen manifest records preparation revision `adc4689e4319d92ae26f3a3ed41729c229b9cc8a`; those are distinct preparation/execution facts. Manifest `/private/tmp/m20-s2-d4-screen-v2/manifest.json`, SHA-256 `fb099e41552ccef42376ed3b9d5a0455b2456d1ddfe0afed8ca8e1a74a1128ac`. Independently verified every file hash declared in that manifest. Reviewed actual Q1–Q4 public response/result, usage, context metadata, frozen ERP and SaaS snapshots, and `/private/tmp/m20-s2-d4-live-aggregate.json`. This was real Nova Pro (`us.amazon.nova-pro-v1:0`) over frozen demo source captures, not a fresh ERP observation. No reviewer model or ERP calls were made.

| Turn | Independent semantic assessment | Logical requests | Cost USD | Reported model workflow latency ms |
|---|---|---:|---:|---:|
| Q1 | Core answer correct: PR6 posted 1 Box, SLE26. Calling the receipt an “order” and the vague next inspection are minor imprecision, not a stop condition. | 4 | 0.0225584 | 11574 |
| Q2 | Preserves the explicit read-only instruction and makes no write claim. Unrequested “arrival facts are empty” is unhelpful wording about the current arrival collection, not evidence that recorded receipts do not exist. | 4 | 0.0229088 | 11399 |
| Q3 | Correct PO quantity 40 Box, cumulative received 2, outstanding 38; both SLE25/26 are real frozen records. Does not infer loss or an incident from the outstanding amount. Suggested warehouse inspection is extraneous; it does not assert an available-to-promise quantity. | 4 | 0.0231760 | 11756 |
| Q4 | **FAIL:** claims independent proof of physical carton contents from receipt, stock-ledger and collaboration records. | 4 | 0.0226216 | 11323 |
| Q5–Q6 | NOT_REACHED after the Q4 stop; no changed-reference/restart semantic result. | — | — | — |

Total observed usage: 16 logical requests, 94,861 input tokens, 4,805 output tokens, USD 0.0912648; all reached turns report zero validation retries and zero source cache hits. Each was below the unchanged USD 0.16 per-question cap. Reported workflow latencies are not end-to-end task duration or a statistically meaningful p95. No costs are assigned to unreached questions. The four reached turns shared conversation ID `7029aabc8acf435db6519d3d0e134f6c`, with context_turns 0/1/2/3 and omitted_turns 0. This has not tested more than three previous turns or the intended restart.

## Q4 failure and causal limits

The decisive assertion is “The receiving basis independently proves what was physically inside the cartons.” The remainder cites posted-stock and collaboration verification and says there is no missing information. Frozen ERP confirms two submitted purchase receipts, each 1 Box, with matching stock entries and balanced accounting. It does not include an independently admitted observation of the contents of either carton. The delivery-note text containing PHOTO identifiers is not an admitted image observation or a counted contents record. Jira completion and the Slack notification are corroborating workflow records, not independent physical measurements.

The current receiving contract already distinguishes this: `agents/receiving_facts.py:model_receiving_source` removes the generic model quantity `physically_arrived`, sets `independently_observed_quantity` only for INDEPENDENT_OBSERVATION, and sets `receipt_confirmed_lower_bound` only for RECEIPT_CONFIRMED. `live_advisory.py:source_payloads` applies this view to `read_erp_evidence`. The observed public projection has RECEIPT_CONFIRMED; therefore the corresponding model view has a null independent quantity and lower bound 2. This conclusion is based on frozen source plus inspected deterministic transformation, not a claim that the public artifact contains a captured wire payload.

The synthesis instructions separately distinguish photo observations and notification copies from authoritative posted-stock records. That instruction concerns posted stock; it cannot make a stock posting prove physical contents. The existing citation/disposition/explanation validation admitted Q4, with no retries. Its narrow notification-name check does not provide general claim-to-evidence reasoning; citing IDs and using SAFE_NOOP are insufficient to certify an epistemic assertion about cartons. The result preserves `write_performed=false` and read-only next action, so the observed failure is unsupported evidence authority/physical inference, not demonstrated external execution.

This is evidence of a semantic accuracy failure despite a qualified fact contract and short retained user context. It does not establish that S2 persistence caused the error, that context compression would fix it, or that another model will pass. Preserve the frozen failure; do not continue Q5/Q6, repeat the same variant, or introduce a question-specific expected-answer/regex patch and call that acceptance. A subsequent mechanism or model experiment needs its own frozen scope and independent gate. The whole requested finalization remains incomplete.
