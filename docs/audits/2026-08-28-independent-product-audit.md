# Independent product audit — 2026-08-28

This record preserves the independent audit findings, the evidence available before
the correction pass, and the bounded dispositions. The product remains a local,
unauthenticated demonstration: the investigation is a scripted Strands harness and
the approval rail uses two distinct simulated role principals. No claim here implies
independent human authentication or a real provider integration.

## Pre-fix findings and evidence

| Finding | Pre-fix evidence | Disposition |
| --- | --- | --- |
| The SSE endpoint closed after a fixed 15-second window while the page could still say LIVE; a future/reset cursor could strand reconnects. | `artifacts/audits/2026-08-28/independent-21-offline-state.png` (when present); server behavior in the pre-fix implementation. | SSE now remains open with repeated heartbeats, emits a typed `stream.reset` when the ledger cursor moves backwards, and the client refreshes the authoritative snapshot before resubscribing. |
| Copilot copy said read-only although recovery language could reach control-plane preparation. | `artifacts/audits/2026-08-28/agents-03-copilot-cited-answer.png`. | Chat has no prepare/approve/execute path. Every turn runs the bounded local read-only Strands/tool harness and records tool/evidence events. Structured decision controls own mutations. |
| The gate could display the latest invoice decision with an older receipt intent and approvals. | `artifacts/audits/2026-08-28/agents-06-stale-gate-after-execution.png`. | The projection binds action, status, approvals, and decision digest to the current intent. A consumed receipt intent is shown separately; the next invoice action starts unprepared with no inherited quorum. |
| Unit inspection called a missing renderer and citation IDs did not always have targets. | `artifacts/audits/2026-08-28/independent-03-dashboard-unit-inspection.png`, `artifacts/audits/2026-08-28/independent-16-citation-focus.png`. | Unit detail rendering is defined and durable evidence IDs render as focusable citation targets. |
| Replay safety appeared before replay and the displayed delta was constant. | `artifacts/audits/2026-08-28/independent-12-recovery-verified-agent.png`. | Replay fields are derived from persisted verification/execution receipts. Before an actual replay they are unavailable/not proven; the effect delta is calculated from authoritative effect counts. |
| Empty chat, keyboard tab navigation, retained scroll, small unit targets, and narrow-width wrapping were weak or misleading. | `artifacts/audits/2026-08-28/independent-20-keyboard-first-tab.png`, `artifacts/audits/2026-08-28/independent-18-mobile-dashboard.png` (when present). | Empty input receives explicit guidance, arrow/Home/End tabs work, view changes reset scroll, unit targets meet 24×24px, and recovery labels wrap within the decision rail. |
| Degraded/invalid browser-smoke PASS rows were hard-coded and URL mode was ignored. | Existing `artifacts/workspace/browser-smoke-v1.json` before regeneration. | Smoke now exercises complete/degraded/invalid modes in the browser and the M7 validator requires UI-driven markers, mode-specific readiness, and fail-closed invalid claims. A PASS artifact must be regenerated by the real smoke run after these changes. |
| Copy implied two independent humans despite one unauthenticated demo client supplying both principals. | `artifacts/audits/2026-08-28/independent-11-two-role-quorum.png`. | Current copy says simulated/distinct role principals and explicitly preserves the unauthenticated demo boundary. |
| Later Copilot turns were keyword-routed without fresh read-tool evidence. | `artifacts/audits/2026-08-28/agents-03-copilot-cited-answer.png`. | Each question launches a fresh deterministic read-only harness namespace; tool, evidence, handoff, synthesis, and evaluation events are persisted. No real provider call was added. |
| Reused public command fingerprints could return an empty result after a crash. | `artifacts/audits/2026-08-28/independent-12-recovery-verified-agent.png` and the durable command-ledger path. | Prepare, approve, execute, and chat re-enter exact envelopes and reconstruct/resume durable work when an in-memory response cache is missing. Changed envelopes fail closed. |
| Exact execution retry could collide on `execution.started` after reservation or enterprise commit. | `tests/contract/test_experiment_session_recovery.py`; the reproducer injects both persistence faults after a prior marker at the prepared case version. | `ExperimentSession` reuses the durable marker's original case version, resumes the idempotent executor, records one effect, and proves verification/replay delta `0`. M7 now runs this proof before accepting a ready audit. |
| Durable chat exact retry could lose its response intent after a reload. | `tests/integration/test_realtime_experiment.py::test_chat_exact_retry_after_reload_preserves_intent`. | The cached durable `copilot.message` payload restores `intent`, message, citations, and `read_only` without touching approval, control-plane, or enterprise state. |
| After receipt restart plus separately authorized invoice release, the final gate could show stale action/status or invent a pending action. | `tests/integration/test_realtime_experiment.py::test_two_action_completion_projects_closed_no_action_gate`; browser flow assertion in `scripts/run_decision_workspace_smoke.py`. | Current intent projection is selected by durable case version and action. Final state is `VERIFIED · CLOSED`, latest action is `Invoice Release`, and no `NEXT ACTION` block or inherited approvals remains. |
| Degraded mode could relabel the page while leaving investigation and Copilot controls available. | Browser exercise of `?mode=degraded` and mode assertions in `scripts/run_decision_workspace_smoke.py`. | Degraded mode hides live investigation, agent graph, hypotheses/traces/evidence, and Copilot; it disables their navigation/input controls and keeps the deterministic safety boundary visible. |
| Narrow Dashboard layout escaped a 390px viewport; tab focus state was not roving. | Browser exercise at 390×844; responsive assertions in `tests-js/scaffold.test.mjs` and `scripts/run_decision_workspace_smoke.py`. | Grid children can shrink, the path diagram owns horizontal scroll, body/document scroll width stays 390px, unit targets are 24×24px, and active/inactive tabs expose tabindex 0/−1 with arrow switching. |
| A fresh page completed the full investigation before a judge could observe Agent work. | Independent judge replay of the normal URL; the first visible state was already `Evaluation ACCEPT`. | Fresh pages now remain at the detected incident with an explicit **Start Investigation** control. The UI projects the actual ordered Harness ledger at a bounded pace, exposing investigator starts, tool calls, evidence, handoffs, synthesis, and evaluation. Completed traces expose an honestly labelled immutable replay. |
| Open-incident replay exited after its first frame while the authoritative API remained complete. | Independent UAT reproduced UI sequence 2 against an unchanged 66-event API ledger. | Replay now stays active until the contiguous ledger queue drains. Independent retest observed sequences 3, 10, 59, 65, and 66; the control re-enabled only at completion, and the API snapshot remained byte-identical. CLOSED replay also passed without regression. |
| Expected SSE client disconnects emitted noisy connection-reset stack traces. | Independent UAT closed the browser while the server was pacing replay frames. | The local server suppresses only `ConnectionResetError` and `BrokenPipeError`; unexpected exceptions still flow to the default server handler. |

## Remaining directional decisions

The following are intentionally not solved by this correction pass: production
authentication/independent-human identity, a real Strands/provider or AgentCore
deployment, a multi-incident product model, remote enterprise writes, and public
actions. Those choices require product/security/operations direction and remain
outside the deterministic local demonstration boundary.

## Record and verification notes

The pre-fix screenshot copies remain in `artifacts/audits/2026-08-28/` so the
correction history is auditable. Fresh final views are in
`artifacts/audits/2026-08-28/post-final/` and show the deliberate idle state, a
three-investigator run in motion, and the completed Agent Workspace.

The canonical real-Chrome smoke now passes start, paced investigation, open and
CLOSED immutable replay, two separately authorized actions, final CLOSED state,
degraded/invalid fail-closed behavior, 390px containment, citations, and zero
remote assets. The final private M7 audit and exact five-minute judge runner pass
with digest `ad222b1365020ab2f5648050ff56331b03f1e999c3a74aa51fd0eb6e15958f67`,
zero provider calls, and zero new provider cost. The full local suite passes 589
tests with 2 skips, plus 10 Node tests, Ruff, formatting, mypy, and
`git diff --check`.

Independent final dispositions:

- User acceptance: **PASS** for all visible controls, API/chart alignment, SSE,
  chat, both approval lifecycles, open/CLOSED replay, mobile behavior, degraded
  mode, and crash/reload recovery.
- Harness truth audit: **PASS**. UI diagnosis is projected from the scripted
  Strands multi-agent harness and its real local tool/evidence/handoff events;
  exact retry remains read-only and idempotent across a reopened session.
- Competition judge: **GO to record**. Projected Stage-Two score is approximately
  **3.94/5**. Publishing and submission remain human-gated release actions.
