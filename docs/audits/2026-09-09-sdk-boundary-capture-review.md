# Frozen receiving SDK boundary capture — independent review

## Initial frozen candidate — BLOCKED pending self-contained tests

Reviewed script SHA-256 `efca029a931daed4de69423670ef63494449c7f82f4b4afe97bf2a9ce0775a3f` and tests SHA-256 `c66c25bb40f007f1de899db646a1d773b054f6f976be464496738149eba860c3`, at reported HEAD `7f586304daf9f04c172e13c232404b6f2a0b8aa3`.

P1: tests require `/private/tmp/m20-s2-d4-screen-v2` and the retained private Q4 artifact. Three tests pass on this workstation (`PYTHONPATH=src:. .venv/bin/python -m pytest -q tests/test_frozen_receiving_sdk_boundary_capture.py`), but they cannot run from a fresh checkout. Replace this dependency with explicit self-contained synthetic fixture documents and retained-question/candidate structure under tmp_path. Do not ship private full source or silently skip tests. The actual Q4 remains the separately reviewed diagnostic CLI input after the code/test gate.

No additional P0/P1 found in the bounded script review. It constructs the local ScriptedStrandsModel only; the imported Bedrock factory is used as a static cast, not instantiated. FrozenFixtureAgentPlatform has no executor and uses file-backed readers. The actual `_invoke` assembles the real Strands Agent/tools, while scripted tool choices drive the five existing read tools. After the result messages reach Model.stream, the local model captures a redacted summary and deliberately stops. Separate retained-candidate injection runs existing validation and explicitly does not claim semantic accuracy. No product prompts, schemas, source transformations or validation rules are changed.

Output is reserved by O_EXCL and chmod0600 before input reads or Strands invocation, then flushed/fsynced. The artifact labels actual current HEAD/SDK, input hashes, historical_wire_capture=false and synthetic orchestration; no assistant answer is copied into its public-facing diagnostic fields. This reviewer ran only the tests, not the actual Q4 CLI, and made no provider or ERP call.

The exact evidence boundary is **current reconstructed post-tool acquisition Model.stream input**. Because this model stops there, the capture does not establish later real final-synthesis input, historical intermediate assistant messages, Bedrock HTTP serialization, or model compliance. A fixed synthetic candidate admitted by existing validation remains a known semantic failure. Tests should verify actual captured message content/history and fail-fast provider/network construction, rather than treating declared zero-provider/human-only booleans as independent proof. Await corrected test candidate before approving the actual private offline capture.


## Corrected candidate — APPROVED for one offline private Q4 capture

Supersedes the test portability blocker above. Reviewed corrected script SHA-256 `ff9088ce49771a7741fa291fcced79f32de4bf616894dd9520487eb57f54f677` and tests SHA-256 `ebf9083d9bc7b6570ac92be79f5d9c6613c8438f20a1510ee41afad0658e227b`. Independently ran `PYTHONPATH=src:. .venv/bin/python -m pytest -q tests/test_frozen_receiving_sdk_boundary_capture.py`: **4 passed**. The tests now construct their own explicitly synthetic source, human history and candidate under tmp_path; no private capture or source file is needed.

The revised tests intercept actual BoundaryCaptureModel.stream messages, find all four original synthetic human requests, and exclude an injected assistant-prose sentinel. They execute with BedrockNovaProFactory.create and socket.create_connection replaced by failures. These guards plus inspection of the explicit local factory/file-reader call path support the zero-provider claim for this diagnostic; they are not a universal network sandbox. Qualified receipt facts are inspected in the actual stream input, with null independently_observed_quantity, lower bound2, and no generic physically_arrived key. A separate injected candidate is admitted by the existing validation seam while the artifact explicitly denies semantic/model-quality acceptance.

The private output reservation tests cover missing input leaving an empty0600 reserved record, and an already-existing output stopping before reconstruction/input reads. The script now records input paths alongside hashes and explicitly sets capture_scope/captured_phase to `post_tool_acquisition_model_stream_before_typed_synthesis`, with `typed_synthesis_input_captured=false`. Source, prompt and production behavior remain unchanged.

**Gate: permitted to run the bounded offline CLI once against the original frozen private Q4/v1 files with a new exclusive private output path.** Independently compare output input hashes to the frozen artifacts afterwards. This gate permits no paid model/provider call, ERP call or product change; it cannot convert Q4 to a pass or claim a historical Bedrock wire capture. No remaining P0/P1 found in this diagnostic scope. Actual CLI evidence remains to be reviewed after execution.


## Actual offline result and final hash reconciliation

Reviewed `/private/tmp/m20-s2-d4-sdk-boundary-capture-reviewed-01.json`, SHA-256 `9c373b7cefa8111299f871a57677076b9ac1d596cfdd5e561e8323f2e3f940cc`. Independently verified all three declared input hashes against the original Q4 artifact, ERP v1 and SaaS files. The artifact reports execution HEAD `7f586304daf9f04c172e13c232404b6f2a0b8aa3`, Strands1.53.0, six local stream calls, the five source results, receipt-confirmed basis, independent quantity null, lower bound2, and no generic physical quantity. It reports zero provider calls and zero external ERP/SaaS calls. The separately injected original Q4 candidate was ADMITTED_BY_EXISTING_VALIDATION with zero retries. That is confirmation of the existing admission gap, not correction of the wrong statement, model behavior, or semantic acceptance.

A freeze race occurred: the approval named script `ff9088…`, while the final script is `0424d427625abbf7c58ae14c736aec24e9a844cfb4f5d97af20bbe2347fd1eea`. Independently proved the exact delta by reversing only the contextlib.suppress import, one explanatory comment and `with suppress(OSError): os.close(descriptor)` exceptional cleanup; the resulting bytes hash exactly to the earlier ff9088 SHA. The final change prevents an already-closed output FD from masking the original write error. It changes no successful capture behavior. Independently reran the four self-contained tests on the exact final0424 candidate: all passed. Tests remain `ebf9083d9bc7b6570ac92be79f5d9c6613c8438f20a1510ee41afad0658e227b`.

There was no pre-run script SHA record for capture01. Its exact historical uncommitted script SHA is therefore **unproven**; neither worker recollection nor file timestamps establish which bytes Python executed. Retain this limitation and do not describe capture01 as exact-approved-SHA execution. Its bounded successful-result facts are consistent with both independently reviewed versions.

The final0424 diagnostic is approved. If an exact frozen-script execution artifact is required, one additional **offline-only** capture is permitted after recording its script/test hashes immediately before execution and verifying them unchanged afterwards, using another exclusive0600 output. This would resolve infrastructure provenance, not retry Q4 with a model or seek a semantic pass. It must retain historical_wire_capture=false and typed_synthesis_input_captured=false. No paid, ERP, or other external calls are permitted by this gate.


## Exact-final-script offline provenance evidence — accepted

Independently inspected `/private/tmp/m20-s2-d4-sdk-boundary-execution-exact-02.json` (SHA-256 `5bcc68be2202bf933323b03ea13b2ed5f1fc3a6be29432f7503a05beb8ac10a7`) and its capture `/private/tmp/m20-s2-d4-sdk-boundary-capture-exact-02.json` (recomputed SHA-256 `7b85e1b48f7e54a7d85ab0b7d964b9ad2ad0eb01636fac73bfe73fdddbdc4653`). The manifest records script0424 and testsebf908 hashes before and after, unchanged, exit0. The expected BoundaryCaptureStopped stderr is the deliberate local post-tool stop, not a provider failure.

The exact capture preserves original input hashes and the same post-tool receipt qualification, five source-result names, zero provider/external source calls, and separate unchanged Q4 candidate admission with zero retries. Actual HEAD7f58630 and SDK1.53.0 are explicit. Full message/invocation hashes differ from capture01 because this is a newly reconstructed local invocation, not historical message replay; no byte-identical historical transcript claim is made.

The bounded diagnostic slice and its exact-final-script offline evidence are accepted. Capture01's freeze-race limitation remains in history; capture02 supplies the planned before/after frozen provenance for this additional offline run. D4 Q4 remains a semantic failure, and final typed-synthesis input, historical Bedrock bytes and general model accuracy remain unproven. No further capture or model retry is needed for this diagnostic deliverable.
