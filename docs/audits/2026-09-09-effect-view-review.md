# Investigation effect relationship view: independent screening review

Final verdict: **FAILED LIVE SCREENING; VARIANT STOPPED AND PRODUCT CHANGE WITHDRAWN**. The initial pre-run approval below was only permission for one diagnostic call, not release or semantic acceptance.

Initial screening review: **APPROVED FOR ONE LIVE SCREENING**, not release acceptance or semantic acceptance. Reviewed the candidate against scope prerequisite `610f355`. No blocking P0/P1 found in the reviewed slice. This review does not approve parallel dialogue, billing or UI work.

Reviewed files: `src/the_missing_20/agents/investigation_effect_facts.py`, the effect-view diff in `src/the_missing_20/agents/live_advisory.py`, `tests/test_investigation_effect_facts.py`, and `scripts/diagnostics/probe_investigation_effect_view.py`. Independent offline execution: `.venv/bin/python -m pytest tests/test_investigation_effect_facts.py -q`, **15 passed**. No model or business writer was invoked by this reviewer.

The helper separates transport response from scoped ERP destination presence. Destination presence uses the canonical correlation's tri-state observation; unavailable, incomplete, wrong ledger/attempt scope and missing business key cannot become authoritative absence. Exact held-lot approval quantities remain raw, while held quantity and transfer read scope/status/presence are grouped for comparison. Unavailable transfer reads retain matching record IDs as observations without promoting them to presence. Source time/revision metadata remains available, and fallback timestamps identify their source.

The integration deep-copies canonical correlation before replacing duplicated model observations. Policy, validation and audit still use canonical findings. Both the reconciliation tool and synthesis context receive the same model view. Receipt conversion factor, normalized amount, source PO revision, scope and original amount remain visible. Other commercial, invoice, supplier, quantity and scan observations remain in the copied correlation; source tools remain available with their original records. No expected verdict, action, eligibility label or policy sentence is introduced by the helper. No prompt instruction or budget setting changed in this slice.

The probe defaults to offline preparation. Explicit `--execute` calls `run_live_advisory` once with the fixed original question and reconstructed source packet; bounded internal repairs remain part of that one invocation. It does not call gateway `ask`, platform progress, or a business writer. It uses the existing gateway factory with incremental and cumulative USD 0.16 caps, and records provider settings and the actual factory ledger in `finally`. The ledger's `incremental_cost_usd` is supported by the reported cost lookup. The output uses exclusive creation and mode 0600 and retains source/code digests and private validation candidates. Accepted runtime output is labeled diagnostic only. Candidate capture limitations are explicitly recorded.

The authorized screening must use the default frozen source, single workflow and original question, with the existing USD 0.16 per-run cap within the experiment's USD 0.30 envelope. Preserve its private artifact and account for actual usage even on validation failure. Stop after this one screen and independently compare its facts, competing hypotheses, authority and next step with the sources before deciding any further experiment. A runtime COMPLETE result alone does not establish diagnosis correctness or fix F03.

Limitations before release: the 15 helper tests establish selected contract counterexamples, not complete live integration or business acceptance. The tool/synthesis identity was inspected in code, not asserted by these helper tests. Independent live semantic review and the appropriate broader offline checks remain gates. Approval is narrowly for learning from one controlled changed-view screening; no claim of a fully accepted model behavior follows.

## Independent live semantic review — FAILED, stop this variant

The authorized single screening is complete. Private evidence: `/private/tmp/m20-effect-view-live-01.json`, including `candidates[0:3]`, `packet_context.effect_facts`, `model_sources`, diagnostics and factory ledger. Do not publish the raw candidates. It returned `AdvisoryValidationError` after eight provider requests, 52,193 input tokens and 2,351 output tokens, costing USD 0.0492776. The root reported 23.84 seconds. The ledger records zero budget errors and USD 0.1107224 remaining; this was not provider authentication or budget exhaustion. All five source readers and reconciliation are listed in each candidate's validation diagnostics. No business writer ran.

The first candidate remained NEEDS_EVIDENCE and substituted investigation of the timeout's cause for resolution of the observed business effect. The second changed to DENY, and the third returned to NEEDS_EVIDENCE, while both later candidates incorrectly claimed the 12-unit attempted receipt was “already recorded” using ERP's 88-unit aggregate. That is a false relationship, not a harmless wording preference. The current scoped destination lookup explicitly reports COMPLETE, pagination complete, matching scope, the attempted business key absent, and no matching record IDs. ERP's 88 units represent other accounted stock, including eight held units; they do not establish this attempted receipt's presence. The third candidate's correct arithmetic that the attempt matches the 12-unit discrepancy does not repair that contradiction.

The exact QA lot also has eight held units, an eight-unit APPROVED record and a COMPLETE exact-lot transfer lookup with presence false. None of the three candidates identifies a genuine missing business prerequisite that overrides these facts. Unknown technical timeout cause remains conflated with unknown destination effect. Neither a generic investigation next step nor a changed disposition is an adequate competing-hypothesis adjudication. Runtime rejection correctly prevented these candidates from becoming accepted advice; no claim of safe or accurate execution follows from merely avoiding a write.

**Final variant verdict: FAILED; stop additional same-variant calls.** The earlier approval was permission to screen, not release approval. Support withdrawing the unaccepted `live_advisory.py` effect-view integration and retaining the helper/tests/probe plus an archived diff as experimental evidence. F03 and overall diagnosis accuracy remain unaccepted.

## Causal limits and a distinct next experiment

The observed failure supports two mechanisms worth distinguishing: insufficient reasoning about record identity versus aggregate quantity, and anchoring on intermediate model prose. The implementation reuses one Agent across acquisition, synthesis and validation repair. Its acquisition phase requests a brief summary; subsequent synthesis receives factual reconciliation but also retains the previous conversation, and repairs retain earlier rejected content. The false record identity appears in the second candidate and persists into the third. This is evidence consistent with repair-history anchoring, but it does not prove a hidden reasoning cause. The artifact does not expose every intermediate acquisition response, so it cannot establish where the belief first arose. One failed packet cannot establish that the provider model is generally incapable of the task.

The smallest distinct mechanism experiment is a **fresh synthesis context**, with the same provider/model, original user request, frozen complete source contents, existing synthesis instruction/schema and original budget. A newly constructed synthesis Agent receives only admitted source results and factual reconciliation from the completed read boundary, without acquisition assistant prose or rejected answers. This is an application-controlled context boundary, not a generated summary, a critic, an expected-disposition hint or an added instructional paragraph. Preserve all source commercial/conversion/QA data and tool provenance; do not replace source results with a handcrafted answer. Capture and independently score the first candidate, then stop. Omit repair for this diagnostic so a new history cannot confound the first-pass comparison; report that restriction explicitly rather than comparing its request count with the existing eight-request repaired run as if they were identical workloads.

Before authorizing that experiment, offline checks must prove the reset removes only model prose, retains every admitted original source payload and the identical schema/instructions, does not fabricate tool execution, and shares the same run's cost ledger and remaining deadline across acquisition and synthesis. Tool-use/result pairing must be valid if original tool messages are imported; do not replay source instructions as system authority. A clean-stage factory must not silently reset budget accounting or gain writer tools. Archive source/message digests and the exact treatment, and freeze counterexamples for absent/present/unavailable destination, wrong scope and mismatched QA quantity before live expansion.

This proposed screen isolates a context-lifecycle mechanism and requires its own implementation review; it is not approval for a model call in this review. If the fresh first-pass candidate still makes the identity error, stop the context hypothesis and consider a separately reviewed capability comparison on frozen cases with a stronger authorized model, holding sources and authority boundaries constant. A successful single fresh pass would justify broader matched testing, not a causal claim or release. Do not add paragraphs, expected enums, a larger budget or a same-model critic to rescue this failed variant.
