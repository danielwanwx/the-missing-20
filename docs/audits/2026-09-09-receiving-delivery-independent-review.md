# Receiving delivery — independent end-to-end review

Date: 2026-09-09 (America/Los_Angeles)
Scope: existing photo/barcode → ERP receiving → Celigo/Slack and Airtable handoff → operational history and current Agent evidence. This is a read-only review. It is not production certification, a complete platform acceptance, or an award prediction.

## Disposition

**HOLD for whole-product release, with one normal physical-to-business path and repeated normal-case Agent runs now accepted.** The fresh dated case proves barcode-before-photo identity binding, one submitted ERP receipt and stock effect, and exactly one Slack-via-Celigo plus one Airtable copy across restart and replay. It does not yet prove the receiving Jira exception lifecycle or Agent behavior across the required exception cases.

**Checkpoint disposition:** the normal one-Box path, barcode-to-current-photo binding, forward timestamp selection, corrected Jira UI labels, and bounded receiving Agent loop are suitable to preserve in the submission branch. The Jira fail-closed journaled lifecycle is still implementation/contract evidence only.

## What is supported by retained evidence

- The retained external verifier records one submitted ERP receipt, `MAT-PRE-2026-00002`, for 1 Box on `PUR-ORD-2026-00012-1`, with a matching +1 stock-ledger effect. It also records one verified Slack notification via Celigo and one verified Airtable row, then the same provider keys after restart. The verifier itself reports zero business writes. See `artifacts/agent/2026-09-08-live-receiving-handoff-readback.json`.
- The local receiving store retains the corresponding capture, human identity confirmation, receipt, and two later barcode lookups. The handoff journal retains exactly one verified row for each of the two configured receipt-notification destinations.
- The handoff implementation requires a submitted receipt and stock-ledger reread before notification (`src/the_missing_20/adapters/receiving_handoff.py:20-71`; `receiving_handoff_worker.py:101-152`). Unknown provider writes become lookup-only, and the worker rechecks the full stock effect before a new notification.
- Barcode lookup is read-only, case/arrival/order-line scoped, validates product mappings against ERP, and does not infer quantity from a common product code (`src/the_missing_20/adapters/receiving_barcode.py:36-114`). Public optical development evaluation is separately approved at 14 correct / 2 abstain / 0 wrong / 0 runtime errors, not as live camera or warehouse accuracy.
- The newly added confirmation path can now bind one selected barcode evidence ID to the current photo digest and image version, re-resolve that code against the current ERP item and PO, and retain the model's original output (`photo_receiving.py:807-899`). This is a sound implementation boundary, but it was added after the retained one-Box transaction and therefore does not change that transaction's provenance.
- A current operator/API checkpoint reports that ERPNext now retains barcode `M20-CARTON-BOX` with UOM `Box` on `M20-DEMO-CARTON`, and that ERPNext's native `scan_barcode` resolves it back to that item/UOM. This closes the earlier empty-master-data setup problem, but this review did not perform the external read itself and no new receipt is attributed to that barcode; preserve the resulting GET payload in the final acceptance artifact.
- The forward history-timestamp correction now chooses the newest server-ingested scan/conflict/barcode/photo timestamp instead of backdating physical evidence to a cached ERP read (`src/the_missing_20/adapters/operational_history.py:245-288`). Its focused physical-evidence tests pass.
- Focused non-HTTP review suite after the barcode-binding and Jira additions: **160 passed** across operational history, barcode, Jira, handoff, destination, and platform-link tests. HTTP tests could not bind a loopback socket in this review sandbox; that environment denial is not counted as a product regression and prior full-suite evidence is kept separate.

## Blocking findings

### Resolved by a new dated transaction — the older transaction was not a barcode → photo → ERP chain

The real receipt was posted around 03:36 UTC. The two retained barcode observations were created later, around 06:30 and 06:40 UTC. The capture still records `identity_confirmation.source = explicit_human_confirmation`; the barcode evidence did not authorize or identify that receipt. The browser artifact likewise says its barcode run requested no stock write.

That is a limitation of the retained evidence, not the latest code path. The current implementation stores barcode results by tenant/case/arrival (`photo_receiving.py:666-735`) and can bind a selected result to the exact current photo digest/image version during explicit identity confirmation, after a fresh ERP barcode and PO-version check (`photo_receiving.py:807-899`). The browser passes the selected evidence only when arrival and item still match (`workspace/photo-receiving.js:415-424`). No retained real transaction has exercised this new causal path yet.

That acceptance has now been performed on the independently dated case. The new retained path binds the earlier barcode observation to the later photo digest/version and current PO version, then records submitted receipt `MAT-PRE-2026-00003`. External GET verification records one matching submitted receipt, one +1 Box uncancelled stock-ledger effect, one Slack-via-Celigo key, and one Airtable key both before and after restart. It also records seven business history points, eight retained rows, unchanged provider keys after restart, and zero verifier writes (`artifacts/audits/2026-09-09-barcode-first-receiving-readback.json`). This supersedes the causal P0 for the normal path; it does not retroactively change the provenance of `MAT-PRE-2026-00002` or prove exception/Jira behavior.

### P0 — Jira create/update/resolve is implemented locally but not yet an accepted destination lifecycle

The older evidence adapter still supplies only the existing Jira GET observation (`src/the_missing_20/adapters/saas_evidence.py:482-561`). A new receiving-specific writer is now integrated behind the explicit `jira_receiving_enabled` runtime switch (`receiving_handoff_worker.py:41-51,132-209`). It creates a case/capture-scoped Task only for `NEEDS_REVIEW`, adds immutable revision comments, and attempts resolution only after a fresh exact ERP receipt/stock-effect reread and both receipt-notification destinations are verified (`receiving_jira.py:104-167`). The earlier malformed-envelope and partial-resolution comparison defects were corrected: provider structures fail as `ValueError`, and every `receipt_event` field must match before even the resolution comment is journaled (`receiving_jira.py:18-29,66-111,183-274`). Direct provider-contract tests cover malformed responses, lost acknowledgements, duplicate identity, per-field resolution mutations, and ambiguous Done transitions.

This is not live acceptance. The current real handoff configuration does not enable or scope the Jira route, the one-Box artifact contains no new receiving issue/comment/Done readback, and the focused tests do not exercise a real worker/provider lifecycle.

The initial judge-facing provenance regression in this new integration has been corrected. `handoffLabel` now classifies Jira before generic destinations and gives verified create/comment/resolve distinct opened/evidence-updated/resolved labels; unknown Jira effects are not presented as verified (`workspace/photo-receiving.js:199-205,286-296`). The executable DOM-independent regression checks all three Jira operations plus Airtable (`tests-js/photo-background-updates.test.mjs:10-19`), and the focused Node file passed 4/4 in this independent review.

Required acceptance: enable only the dedicated demo project and run one real scoped exception through create → evidence revision → authoritative ERP recovery → Done reread → restart, proving no duplicate issue/comment/transition. Retain provider IDs and timestamps.

### P1 — Existing retained history still contains pre-fix causal timestamps

The forward code fix is sound in the focused tests, but the append-only real database still contains barcode-bearing history points whose `observed_at` precedes their barcode records: history rows 9/10 are about 06:30:25 and 06:39:51, while their barcode observations are about 06:30:55 and 06:40:18. Those old rows are deliberately not rewritten.

Required acceptance: create one new physical event after the fix and verify the stored point time is at or after its server ingestion/observation time, unchanged polling creates no point, and the UI orders it correctly. Keep the old rows with an explicit software-timestamp limitation or revision record; do not silently edit them into apparent original facts.

### P1 — One real Agent sequence passes, but repeated-run stability is not accepted

The older retained Strands/Bedrock conversation demonstrates source tools, exact ERP/SaaS/barcode citations, and read-only behavior, but also retains the previously documented trend-language errors. A new three-turn Nova Pro sequence now completes against the dated receiving case (`artifacts/audits/2026-09-09-real-receiving-conversation.json`). It distinguishes 1 posted Box from 39 outstanding/unarrived Boxes; refuses a retry and all inventory writes; cites the PO, receipt, ledger, photo and notification records; reads actual operational history; explains the 0→1 net change and the separate prior-observation mean; and explicitly rejects revenue-gain attribution. The attached chart contains seven retained business observations with exact 0→1 values. All three results are `SAFE_NOOP`, `write_performed = false`, with no application validation retries or source-cache hits. Their latencies are 18.339 s, 11.021 s and 12.775 s, and their total metered incremental cost is about USD 0.117.

That successful sequence is useful real-model evidence, not a stability proof. The next preserved repeat failed on its first turn with `AgentBudgetExceeded` after four requests and about USD 0.040 actual metered cost (`artifacts/audits/2026-09-09-real-receiving-conversation-repeat.json`). The gateway's former USD 0.08 hard cap could not reserve another conservatively estimated byte-as-token request. The follow-up code raises only the per-run cost caps to USD 0.16 while retaining the 16-request, 250k-input-token, 12k-output-token, per-call timeout and no-fallback boundaries (`src/the_missing_20/adapters/live_advisory_gateway.py:457-486`). Compact JSON source payloads and an explicit receiving synthesis instruction reduce unnecessary rereads without removing evidence (`src/the_missing_20/agents/live_advisory.py:1590-1596,1722-1744`).

The first post-cap run no longer failed its budget, but it still did not pass: its first candidate cited an ID outside the admitted set, and the repair returned invalid structured data, so the gateway correctly withheld the conclusion (`artifacts/audits/2026-09-09-real-receiving-conversation-final-a.json`). The latest prompt change gives the typed receiving phase only the literal IDs that are both admitted and present in a source actually read; it does not disclose the expected disposition or a canned answer, and the unchanged server validator remains authoritative (`src/the_missing_20/agents/live_advisory.py:1747-1769`). Its focused contract asserts that only the read `PO`/`PR` IDs are exposed and that `SAFE_NOOP` is absent from this structured-output instruction (`tests/test_evidence_completion_hook.py:76-121`). This is a sound bounded correction, but no post-correction real pass exists in the reviewed artifacts yet. Focused gateway/advisory/view tests pass; both failed artifacts must remain visible until consecutive real runs pass.

A later post-citation-change run still does not satisfy that gate (`artifacts/audits/2026-09-09-real-receiving-conversation-citation-a.json`). Its first turn completes with admitted literal IDs and a correct 40/1/39 safety conclusion, but unnecessarily repeats trend, baseline and revenue language from the retained three-turn transcript even though the newest question did not ask for it. Its second turn reads all six relevant tools and then returns no structured result; the gateway correctly reports `VALIDATION_FAILED` and exposes no conclusion. This shows the citation correction is effective but multi-turn answer focus and structured-output reliability remain unresolved in the real runtime.

The next diagnostic run makes the SDK failure precise (`artifacts/audits/2026-09-09-real-receiving-conversation-stop-diagnostic.json`). Its first two turns complete without stale trend/revenue content, while the history turn returns native `stop_reason = limit_total_tokens` after all source reads. Strands `Limits.total_tokens` counts cumulative full-history processing inside one invocation; it is not the provider's per-call output maximum. The latest patch raises only the receiving typed-synthesis SDK envelope to 80k cumulative tokens and four provider-sized output allotments. The factory ledger remains the independent USD 0.16 / 250k input / 12k output / 16-request / timeout boundary (`src/the_missing_20/agents/live_advisory.py:318-326,1769-1777`; `src/the_missing_20/adapters/live_advisory_gateway.py:459-486`). A no-output native `limit_*` stop is now classified as bounded unavailability with a public stop reason, not as an unsafe model conclusion or generic validation failure (`src/the_missing_20/agents/live_advisory.py:1810-1818`; `src/the_missing_20/adapters/live_advisory_gateway.py:28-33`). Focused tests cover both total/output stops and passed in this review. This is **APPROVE as a bounded diagnosis/correction**.

Answer quality is not fully accepted even in that diagnostic run: turn 2 says the receipt was verified by ERP, Airtable, Celigo and collaboration tools. The receiving prompt correctly says ERP/ledger prove stock and the other systems are notification copies (`src/the_missing_20/agents/receiving_advisory.py:190-191`), but the validator did not reject this authority conflation. Final evidence should say that ERP receipt plus stock ledger are authoritative, while photo and downstream records corroborate the workflow; Airtable/Slack/Celigo must not be described as independent stock verification.

A first post-limit three-turn run now completes all turns within the new bounds (`artifacts/audits/2026-09-09-real-receiving-conversation-native-limits-a.json`). This proves the specific native-limit correction once, but does not close the quality gate. Turn 1 again answers unasked history/revenue content and prints the prior mean as unrounded `0.14285714285714285`, contrary to the newest-question and two-decimal instructions. Turn 2 repeats the notification-authority conflation. Turn 3 is arithmetically consistent with the actual attached cohort: six prior zeroes, one prior value of one, one non-comparable null, and latest one produce a prior comparable mean of 1/7 and a first-to-latest net change of +1 Box.

A second post-limit sequence also completes mechanically (`artifacts/audits/2026-09-09-real-receiving-conversation-focused-a.json`), but repeats both defects: its first self-contained 40/1/39 question includes the entire unrequested trend/revenue discussion, and its second answer again treats notification copies as receipt-verification authorities. The prompt-only "newest question" instruction has therefore failed twice. A clean acceptance needs a structural context boundary: self-contained questions must not inherit unrelated prior topics, and history tools/history-specific synthesis should be offered only for an explicit history/trend request. These are two consecutive technically complete sequences, not two consecutive clean judge-quality sequences.

That structural boundary is now implemented. Receiving turns retain prior human questions for reference resolution but omit old assistant prose; the history reader is removed unless the newest question has explicit history intent; typed synthesis receives only the newest question; and the application validator rejects the exact observed pattern that treats notification systems as stock authority (`src/the_missing_20/adapters/live_advisory_gateway.py:271-293,377-399`; `src/the_missing_20/agents/live_advisory.py:1521-1528,1733-1749,1938-1954`). This is stronger than another prompt reminder and keeps the read-only constraint in the packet/validator.

The semantic-role guard is deliberately narrow evidence for the reproduced sentence, not a complete natural-language proof: its global keyword condition could still miss a mixed sentence that labels one system a notification and another system authoritative. Exact evidence-ID admission and deterministic stock checks remain the real correctness boundary; this prose guard is judge-facing quality hardening.

The first real run through that boundary completes all three turns (`artifacts/audits/2026-09-09-real-receiving-conversation-isolated-a.json`). Turn 1 stays on the 40/1/39 receiving question and does not call history. Turn 2 initially triggers one bounded validation repair, then correctly names ERP receipt and stock ledger as authority and rejects retry. Turn 3 calls history, returns an actual attachment and rejects ROI attribution. It explains the distinction and gives net +1 Box but omits the requested numeric prior mean/difference.

A consecutive second run also completes all three turns, now with zero repairs (`artifacts/audits/2026-09-09-real-receiving-conversation-isolated-b.json`). It preserves the 40/1/39 interpretation, ERP/ledger authority, no-retry/read-only boundary, real history attachment, net +1 Box, prior-average difference 0.8571 Box and no-ROI conclusion. Formatting remains a minor presentation issue (one or four decimals rather than the requested two), but the two post-structural runs close the normal-case repeated-runtime gate. They do not establish accuracy on mismatch, damage, over-receipt or Jira recovery.

Required acceptance: run a preregistered fresh set covering barcode/photo mismatch, duplicate/lost-ACK, quality/damage stop, over-receipt, and Jira resolution. Score answer facts, citations, stop decisions, writes, provider effects, latency and cost separately; retain failures. Do not treat the repeated normal partial-receipt sequence as overall Agent reliability.

## Evidence boundaries that must remain visible

- The successful one-Box photo was a public development image plus explicit human SKU confirmation, not a real commercial shipment.
- The current barcode camera/video artifact uses synthetic video and reports `camera_hardware_tested: false` and `stock_write_requested: false`.
- Operational history is retained observation-time history, not a complete ERP event ledger, industry benchmark, labor study, or causal ROI proof.
- Local SQLite idempotency is appropriate for the bounded single-deployment demo; `receiving_handoff.py` explicitly does not claim distributed exactly-once delivery.
- Passing unit/contract tests does not replace a fresh real Jira lifecycle, a fresh post-barcode receipt, or a fresh external readback.

## Minimum path to bounded delivery acceptance

1. Enable the bounded Jira route and perform one real create→comment→resolve→readback lifecycle.
2. Provision one fresh isolated arrival and execute scan→photo→explicit approval→ERP receipt/SLE→Celigo/Slack+Airtable (+ Jira when exceptional) in causal order.
3. Confirm the post-fix history timestamp and dedup behavior on that event, preserving the older limitation.
4. Run fresh Agent questions before and after each state change and score the original answers; no unsupported invoice, quality, revenue, delivery, or completion claims.
5. Save provider IDs, timestamps, source versions, UI screenshots, and zero-duplicate restart readback in one acceptance artifact.

Until those gates pass, the supported statement is: **one barcode-bound, human-confirmed one-Box receipt and its two notification copies are real and replay-safe in the bounded demo, and the normal receiving Agent dialogue now has two consecutive real runs; the Jira exception lifecycle exists in code but is not yet proven as a complete real transaction.**

## Later checkpoint: independently dated order provisioning

The follow-up provisioning change is narrowly scoped and preserves the old order rather than amending or reusing it. Valid case IDs receive a case-specific immutable line marker and disjoint handling-unit IDs; discovery explicitly skips other recognized pilot markers and still fails closed on truncation, ambiguity, malformed markers, wrong company/supplier, order terms, business date, or amendment linkage (`scripts/provision_goods_demo.py:27-31,34-84,87-248`). The focused provisioning suite passed **26/26** in this review. The retained creation artifact records submitted PO `PUR-ORD-2026-00013`, 40 Box at explicitly synthetic USD 50/Box, case `M20-GOODS-20260909-40`, and a one-Box first-arrival manifest, with no inventory write claimed (`artifacts/audits/2026-09-09-new-goods-order.json`). This is a valid pre-receipt checkpoint, not proof of the downstream workflow.

### Resolved — Case date now matches the ERP business date before access

The initial review found that the dated case marker and ERP business date could disagree. `provision` now compares the case's embedded date with `business_date` before checking the environment or calling either ERP method (`scripts/provision_goods_demo.py:87-101`). The new regression replaces both `_document` and `_request` with forbidden functions and proves that a mismatch stops before any ERP access (`tests/test_goods_receiving_provision_scope.py:245-255`). The independent rerun passed **27/27**, and Ruff passed. This provisioning increment is **APPROVE** within its narrow scope.

### New live checkpoint — causal binding and normal downstream verification are real

The isolated runtime now retains barcode observation `barcode:697228…` at 08:18:32 UTC, photo digest `2dc3d1…` uploaded at 13:41:37 UTC, actual Nova Pro analysis (4.823 s, 5,624 tokens, original `item_code` empty), and explicit operator confirmation at 13:42:42 UTC. The confirmation stores `source = operator_confirmed_barcode_photo`, the exact barcode evidence ID, photo digest/version, and matching PO version. This is the first retained evidence in this review where the barcode genuinely precedes and binds the photo; it closes the earlier causality gap through the confirmation boundary.

After this checkpoint, the same local capture advanced through verified draft and receipt submission for `MAT-PRE-2026-00003`. The final external readback now verifies the exact +1 stock effect plus one Slack-via-Celigo record and one Airtable record, with the same receipt, ledger semantics and provider keys after restart (`artifacts/audits/2026-09-09-barcode-first-receiving-readback.json`). This closes the earlier normal-path downstream checkpoint. Jira is correctly absent because this was not a `NEEDS_REVIEW` case; its exceptional lifecycle remains a separate release gate.

### Resolved for current reads/charts — mixed transition evidence is retained but excluded

The new history row observed at 13:43:47 UTC contains `receiving_refs.receipt_name = MAT-PRE-2026-00003` and `posted_quantity = 1`, while the same normalized point still labels that receipt `DRAFT` and records `metrics.received = 0` / `receipt_posted_quantity = 0`. The photo store already records `RECEIPT_SUBMITTED` and exact stock-ledger verification at that time. This is reproducible from the retained SQLite stores and follows the current composition behavior: `_read_all` combines a possibly 30-second cached ERP projection with current local receiving work (`agent_platform.py:1767-1854`; `erpnext_source.py:637-651`), while `_observation` advances the timestamp to the newest local physical event (`operational_history.py:245-290`).

The follow-up fix now detects a local posted receipt missing or quantity-mismatched in the aggregate ERP snapshot, prevents `CURRENT` freshness and metrics, forces one cache invalidation/reread when the source supports it, and never writes a newly detected mixed point (`operational_metrics.py:98-154`; `agent_platform.py:1767-1785,1848-1859`; `operational_history.py:408-412`). Query compatibility retains the old raw row but removes it from charts/baselines and discloses record ID, time, receipt and reason (`operational_history.py:95-101,493-552`). A read-only query against the actual runtime returns coherent points 1-6 and 8, explicitly excludes row 7 as `RECEIVING_RECEIPT_NOT_REFRESHED`, and leaves retained coverage at eight records. This scoped correctness fix is **APPROVE**.

### Resolved — Persistent conflict refresh is bounded across UI polls

The first revision bounded refresh only inside one `_read_all` call, so a persistently stale ERP snapshot could make each Dashboard poll bypass the provider cache. `AgentPlatform` now keeps a monotonic 30-second refresh deadline and advances it before invalidation/external I/O (`agent_platform.py:46-67,1769-1789`). During the cooldown, a continuing mismatch remains `UNAVAILABLE`, metrics/history remain suppressed, and ordinary source caching is preserved.

The new regression makes three consecutive reads against a persistently stale source and observes one invalidation, advances the monotonic clock past 30 seconds and observes exactly one more, then supplies an authoritative snapshot and restores `CURRENT` without another invalidation (`tests/test_receiving_platform_link.py:146-168`). The broader focused group passed in this independent rerun. This limited fix is **APPROVE**.

Query compatibility currently excludes the actual old DRAFT row correctly. It identifies legacy mixed rows by a local receipt absent from the set of posted document names; because normalized historical document references do not retain per-receipt quantities, an older row with the same posted name but a mismatched quantity cannot be detected exactly at read time. The guarded writer prevents that shape going forward. Treat this as a bounded legacy limitation unless quantity-bearing history is added without rewriting old evidence.

### Acceptance-harness notes

The first three-turn conversation script ended after otherwise successful live turns because it expected `context_turns` to increase without bound. The gateway deliberately retains at most three prior entries, including failed requests, so the observed `2, 3, 3` is the intended bounded-memory behavior. The corrected assertion saturates at three (`scripts/diagnostics/verify_receiving_conversation.py:60-66`); this original harness failure is not an Agent-context defect.

The external readback artifact proves restart-stable receipt, stock, Slack/Airtable keys and the original seven-point history checkpoint. A machine-readable local replay artifact records three HTTP submits with the same receipt and capture version plus a rejected duplicate-photo/retake request with HTTP 400 (`artifacts/audits/2026-09-09-posted-receiving-replay.json`). A later `after_replay` phase in the external readback now confirms the same single ERP receipt, same +1 ledger effect, and same one Slack/Airtable keys with zero verifier writes (`artifacts/audits/2026-09-09-barcode-first-receiving-readback.json`). History has legitimately advanced to nine business points / ten retained rows because of source outage/current transitions; the artifact explicitly discloses that change instead of pretending history is frozen. The local plus external replay gate is therefore closed for this one normal transaction.

### Current release gates

1. Preserve the two consecutive post-structural normal-case runs and all earlier failures; require the same grounding standard on the exception run.
2. Execute one real bounded `NEEDS_REVIEW` Jira create → evidence update → authoritative recovery → resolve → restart/readback lifecycle, with duplicate counts.
3. Exercise and retain evidence for barcode/photo mismatch, duplicate/lost acknowledgement, damage/quality stop and over-receipt; the successful normal partial receipt does not cover those stop paths.
4. Preserve the completed local plus post-replay external proof for this transaction; repeat the same no-duplicate check when the exceptional Jira path is exercised.
5. Keep public-photo/synthetic-order and observation-history limitations visible; do not call the normal path a commercial shipment, production deployment, causal ROI proof or award guarantee.

### Resolved in code — receiving dialogue now acquires evidence before typed synthesis

The live `1 / 40` receiving question exposed an SDK-mode mismatch: forced structured-output synthesis could hide the ordinary source tools before the model had completed the five required reads. The receiving path now uses the same bounded untyped acquisition boundary as source investigation: it derives the required tool set from the admitted receiving packet, permits at most two acquisition turns under the original shared wall-clock and token budgets, and starts typed synthesis only after all required reads are present (`src/the_missing_20/agents/live_advisory.py:1651-1697`). The final application-owned validator still requires the same complete tool set and restricts citations to IDs returned by sources actually read (`src/the_missing_20/agents/live_advisory.py:1849-1884`).

The incomplete branch fails closed before constructing a `LiveAdvisoryResult`: it raises `AdvisoryUnavailable`, and both chat and investigation gateways return `result = None` with an explicit unavailable status rather than a fallback diagnosis (`src/the_missing_20/adapters/live_advisory_gateway.py:287-310,417-441`). The new contract test exercises both paths: completion requires two untyped acquisitions followed by typed synthesis; repeated partial acquisition stops after two turns and never reaches typed output (`tests/test_evidence_completion_hook.py:76-119`). An independent rerun of the evidence-hook, receiving-advisory, gateway and operational-semantics files passed **83/83**.

This increment is **APPROVE within its code/test scope**. It proves the previously blocked SDK phase transition and the no-fabricated-conclusion failure boundary; it does **not** prove that a fresh Bedrock run will produce a high-quality answer. The platform disposition remains **HOLD** until the real rerun and the other live acceptance gates above are retained.

Non-blocking observability debt: when acquisition performs some reads and then exhausts both turns, `_invoke` retains usage and a generic acquisition diagnostic but not the partial `calls` list (`src/the_missing_20/agents/live_advisory.py:1762-1776`), while the gateway deliberately returns `tool_calls = []` on unavailable responses (`src/the_missing_20/adapters/live_advisory_gateway.py:299-310`). This does not fabricate a conclusion or weaken safety, but a future judge-facing failure trace should distinguish “no tools ran” from “partial evidence acquisition failed.”
