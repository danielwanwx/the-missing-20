# Model capability and access preflight

Status: READ/DIAGNOSTIC FINDINGS; NO ALTERNATE MODEL OR IAM CHANGE ACCEPTED. Existing Nova Pro remains the product model. The user explicitly authorizes competition-demo ERP/SaaS evidence to be sent to Bedrock and requests demo access needs be enabled.

The assumed demo role's sole inline policy currently permits only Nova Pro invocation. Model availability queries on that role returned AccessDeniedException. Read-only inspection through its documented source login confirmed Nova2Lite agreement, authorization, entitlement and region availability, and an ACTIVE US profile targeting us-east-1/us-east-2/us-west-2. Sonnet4.6 authorization/entitlement/region were available but agreement was NOT_AVAILABLE; no agreement or subscription was changed. These are metadata findings, not invocation success.

Prepared a separate `Missing20DemoModelEvaluation` role policy: five Bedrock model/profile information reads and exact Nova2Lite profile/model invocation resources; preserve the existing NovaPro policy. Automatic approval review rejected applying it because it requires explicit user approval of the specific persistent policy/resource scope. The approval question is pending. The policy has NOT been applied, and no alternate profile or indirect action is being used to bypass that rejection. Existing-authorized NovaPro and independent offline work continue. Private exact before/policy/availability evidence is under `/private/tmp/m20-bedrock-role-policy-before.json`, `/private/tmp/m20-demo-model-evaluation-policy.json`, and `/private/tmp/m20-model-availability-source-profile.json`.

Official [Sonnet4.6 model card](https://docs.aws.amazon.com/bedrock/latest/userguide/model-card-anthropic-claude-sonnet-4-6.html) lists Converse, US geo profiles and regional availability, which does not establish this account's agreement/invocation access. Official [Nova2 Converse documentation](https://docs.aws.amazon.com/nova/latest/nova2-userguide/using-converse-api.html) describes reasoning disabled by default, low/medium/high effort when enabled, and hidden reasoning tokens still billed. The [AWS equipment assistant walkthrough](https://aws.amazon.com/blogs/machine-learning/build-an-ai-powered-equipment-repair-assistant-using-amazon-bedrock-agentcore/) cites Nova2Lite USD0.30/M input and2.50/M output; refresh/confirm exact regional pricing before a paid comparison and record all reasoning output tokens. Do not silently use NovaPro prices in the ledger for a different model.

The [Nova v1 complete request schema](https://docs.aws.amazon.com/nova/latest/userguide/complete-request-schema.html) also mentions reasoningConfig for Pro/Lite, while its linked Converse walkthrough has no such example. One bounded existing-model capability request tested the documented top-level `additionalModelRequestFields.reasoningConfig={type:enabled,maxReasoningEffort:low}` with NovaPro, temperature0,128 max output tokens and only the synthetic text `Reply OK.`. Bedrock returned ValidationException: provided reasoning config value invalid. Exact private result: `/private/tmp/m20-novapro-reasoning-capability-probe.json`. This establishes that tested configuration was rejected, not that every possible reasoning configuration is unsupported. No token usage was returned; do not claim a measured zero charge. No real ERP data or business writes were involved. No variant retry followed.

Before a distinct model-capability experiment: freeze supported model/configuration, source/question/system/schema/workflow, caps and model-specific pricing; test SDK tool/schema and budget accounting offline; then screen once with independent semantic review. Fresh-context and relationship-view variants remain stopped. A new model/configuration's success cannot be inferred from product descriptions or an IAM permission.

## Independent findings and permission-boundary review

The report's evidence distinctions are sound. Read the private prepared policy, model-availability response and NovaPro reasoning rejection locally. The policy contains five model/profile information reads plus InvokeModel/InvokeModelWithResponseStream scoped to the stated Nova2Lite profile and its three destination-region model ARNs. This describes the proposed policy; it is not evidence it was applied. The availability JSON supports Nova2Lite's available agreement and the distinct Sonnet4.6 NOT_AVAILABLE agreement despite other available/authorized metadata. Neither establishes a successful model invocation.

The capability artifact records exactly one synthetic Reply OK probe with128 output tokens and the stated low reasoning configuration, rejected with ValidationException. It contains no usage measurement. The narrow conclusion and absence of a zero-charge claim are appropriate; do not generalize this rejection to all Nova reasoning support. Pricing cited from an example walkthrough remains a lead requiring an official current model/region price check before a new ledger configuration, as the report already requires.

**Review verdict: APPROVED as a findings/limitations document, not as IAM or model-experiment authorization.** The automatic approval rejection remains operative and the specific persistent policy/resource approval remains pending; preserve that boundary. Do not infer approval from this review, general demo-data permission, availability metadata, another credential/profile or elapsed time. Continue independent authorized work while the dependent IAM step waits. No policy/subscription/profile change, model request or external operation was made by this reviewer, and the private access artifacts must not be published as public credential/runtime dumps.

## Additional official compatibility check

Reopened the [Nova2Lite model card](https://docs.aws.amazon.com/bedrock/latest/userguide/model-card-amazon-nova-2-lite.html) September9 Pacific. It lists Converse/Invoke and client-side tool calling, while native Bedrock structured outputs are marked unsupported. Strands' tool-based structured result is a separate mechanism and needs an offline compatibility check; do not equate that SDK feature with native provider structured output support. The US geo profile ID remains `us.amazon.nova-2-lite-v1:0`. The [official pricing page](https://aws.amazon.com/bedrock/pricing/) was reopened, but the extracted Amazon Nova dynamic price tables contained no model-rate rows; exact regional pricing is therefore still unverified by that read, not silently accepted from a search snippet. No additional model call, configuration change or IAM mutation followed.
