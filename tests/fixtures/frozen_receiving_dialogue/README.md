# Frozen receiving dialogue fixture

These files are fully synthetic, read-only test inputs. They contain no private
capture, credentials, external URLs, personal data, or live ERP/SaaS records.
`erp-v2.json` changes only the synthetic ledger label needed to exercise the
restart reread path.
