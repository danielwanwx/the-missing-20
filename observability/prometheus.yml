global:
  scrape_interval: 2s
  evaluation_interval: 2s

scrape_configs:
  - job_name: missing20-experiment
    metrics_path: /metrics
    static_configs:
      - targets: [host.docker.internal:8765]
